{
  "name": "slack-andreid",
  "version": "1.0.0",
  "description": "Andreid on Slack",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "start": "node --require dotenv/config app.js",
    "start:20": "node --env-file .env app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "@slack/bolt": "^4.2.0"
  },
  "devDependencies": {
    "dotenv": "^16.4.5"
  }
}
